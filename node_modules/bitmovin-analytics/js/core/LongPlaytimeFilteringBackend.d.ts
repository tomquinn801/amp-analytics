import { Sample } from '../types/Sample';
import { Backend } from './Backend';
export declare class LongPlaytimeFilteringBackend implements Backend {
    private backend;
    constructor(next: Backend);
    sendRequest(sample: Sample): void;
    sendUnloadRequest(sample: Sample): void;
    sendRequestSynchronous(sample: Sample): void;
    sendAdRequest(sample: any): void;
    private isPlayedAboveThreshold;
}
