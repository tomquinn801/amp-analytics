import { AdSample } from '../types/AdSample';
import { Sample } from '../types/Sample';
export declare class AnalyticsCall {
    private analyticsServerUrl;
    private adAnalyticsServerUrl;
    constructor(baseUrl: string);
    sendRequest(sample: Sample, callback: () => void): void;
    sendUnloadRequest(sample: Sample): void;
    trySendBeacon(fallbackFunction: (data: any) => void, url: string, data: any): void;
    sendRequestSynchronous(sample: Sample, callback: () => void): void;
    sendAdRequest(sample: AdSample): void;
    getAnalyticsServerUrl(): string;
    getAdAnalyticsServerUrl(): string;
}
