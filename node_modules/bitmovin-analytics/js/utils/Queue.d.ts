export declare class Queue<T> {
    private readonly _items;
    get items(): ReadonlyArray<T>;
    get length(): number;
    offer(item: T): void;
    poll(): T | undefined;
    clear(): void;
    limit(maxElements: number): void;
}
