import { SegmentInfo } from '../types/SegmentInfo';
export declare class SegmentTracker {
    protected segments: SegmentInfo[];
    constructor();
    reset(): void;
    addSegment(segment: SegmentInfo): void;
    removeSegment(url: string): void;
    getSegments(): SegmentInfo[];
}
