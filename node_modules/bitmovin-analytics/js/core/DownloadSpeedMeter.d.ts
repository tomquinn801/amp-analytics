import { DownloadSpeedInfo } from '../types/DownloadSpeedInfo';
import { SpeedMeasurement } from '../types/SpeedMeasurement';
import { Measure } from './Measure';
export declare class DownloadSpeedMeter {
    protected measures: Measure[];
    constructor();
    reset(): void;
    addMeasurement(measurement: SpeedMeasurement): void;
    getInfo(): DownloadSpeedInfo;
    private add;
    private avgSpeed;
    private bytePerSecondToBitPerSecond;
    private minSpeed;
    private speeds;
    private maxSpeed;
    private totalTime;
    private avgTimeToFirstByte;
}
